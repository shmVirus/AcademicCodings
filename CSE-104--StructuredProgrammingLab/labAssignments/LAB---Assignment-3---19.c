#include<stdio.h>
int main(void)
{
    float a,b, x,y;
    scanf("%f", &a);
    if (a>=0 && a<=50)
    {
        y = a*0.50;
        x = y + ((y*20)/100);
    }
    if (a>50 && a<=150)
    {
        b = a - 50;
        y = 50*0.50+b*0.75;
        x = y + ((y*20)/100);
    }
    if (a>150 && a<=250)
    {
        b = a - 150;
        y = 50*0.50+100*0.75+b*1.20;
        x = y + ((y*20)/100);
    }
    if (a>250)
    {
        b = a - 250;
        y = 50*0.50+100*0.75+100*1.20+b*1.50;
        x = y + ((y*20)/100);
    }
    printf("%.2f\n", x);
    return 0;
}
